- name: Create {{ migration_sample_name }} SCC
  k8s:
    host: "{{ url }}"
    api_key: "{{ token }}"
    verify_ssl: no
    state : present
    definition: "{{ lookup('template', 'mssql-scc.yml.j2' )}}"
